import styled from 'styled-components';

function Profile() {
  return <ProfileWrapper>프로파일</ProfileWrapper>;
}

const ProfileWrapper = styled.div`
  width: 100%;
  border: 1px solid black;
`;

export default Profile;
