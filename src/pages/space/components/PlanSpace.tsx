import styled from 'styled-components';

function PlanSpace() {
  return <PlanSpaceWrapper>플랜스페이스</PlanSpaceWrapper>;
}

const PlanSpaceWrapper = styled.div`
  width: 100%;
  border: 1px solid black;
`;

export default PlanSpace;
